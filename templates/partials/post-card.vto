<article class="post-card">
  {{ if post.frontmatter.date }}
    {{ include "partials/post-meta-dates.vto" { page: post, dateClass: "post-card__date" } }}
  {{ /if }}
  <h2 class="post-card__title">
    <a href="{{ post.url }}">{{ post.frontmatter.title }}</a>
  </h2>
  {{ include "partials/post-meta-tax.vto" { categories: post.frontmatter.categories ?? [], tags: post.frontmatter.tags ?? [], page: post, listingUrl: listingUrl } }}
  {{ if post.excerpt }}
    <p class="post-card__excerpt">{{ post.excerpt }}</p>
  {{ /if }}
</article>
